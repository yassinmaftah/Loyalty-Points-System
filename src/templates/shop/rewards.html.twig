<!DOCTYPE html>
<html>
<head>
    <title>My Rewards</title>
</head>
<body>
<nav style="background: #f4f4f4; padding: 10px; margin-bottom: 20px;">
    <a href="/Loyalty-Points-System/">Home</a> | 
    <a href="/Loyalty-Points-System/shop">Boutique</a> | 
    <a href="/Loyalty-Points-System/cart">My Cart</a> | 
    <a href="/Loyalty-Points-System/rewards">Rewards</a> | 
    <a href="/Loyalty-Points-System/history">My History</a> | 
    <a href="/Loyalty-Points-System/logout" style="color: red;">Logout</a>
</nav>
    <h1>Redeem my points</h1>
    <p>Your current balance : <strong>{{ user.loyalty_points }} points</strong></p>

    <div style="display: flex; gap: 20px;">
        {% for reward in rewards %}
            <div style="border: 1px solid #ccc; padding: 15px;">
                <h3>{{ reward.name }}</h3>
                <p>{{ reward.description }}</p>
                <p>Points required : <strong>{{ reward.points_required }}</strong></p>
                
                <form action="/Loyalty-Points-System/shop/redeem" method="POST">
                    <input type="hidden" name="reward_id" value="{{ reward.id }}">
                    <button type="submit" {% if user.loyalty_points < reward.points_required %}disabled{% endif %}>
                        To exchange
                    </button>
                </form>
            </div>
        {% endfor %}
    </div>

    <br>
    <a href="/Loyalty-Points-System/shop">Retour à la boutique</a>
</body>
</html>